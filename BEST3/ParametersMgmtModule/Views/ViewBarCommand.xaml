<UserControl x:Class="ParametersMgmtModule.Views.ViewBarCommand"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ParametersMgmtModule.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    <Grid Margin="5">
        <StackPanel Orientation="Vertical">
            <Button Margin="1" Content="Overview" Width="100" Command="{Binding ViewCommands.OverviewCommand}" />
            <Button Margin="1" Content="Monitoring" Width="100" Command="{Binding ViewCommands.MonitoringCommand}" />
            <Button Margin="1" Content="Configuration" Width="100" Command="{Binding ViewCommands.ConfigurationCommand}" />
            <Button Margin="1" Content="Alarms" Width="100" Command="{Binding ViewCommands.AlarmCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
